{
  "entities": {
    "Feedback": {
      "title": "Feedback",
      "description": "Privacy contract for user suggestions, ideas, and bug reports. Runtime public submissions use the Prisma-backed /api/feedback route as the source of truth.",
      "type": "object",
      "properties": {
        "userId": { "type": "string", "description": "The ID of the user who submitted the feedback" },
        "userName": { "type": "string", "description": "The public pseudonym or display label of the user" },
        "brainLabel": { "type": "string", "description": "Short non-portable Brain label for admin display; never the raw Brain ID" },
        "content": { "type": "string", "description": "The feedback content" },
        "type": { "type": "string", "enum": ["idea", "bug", "improvement", "other"], "description": "The category of feedback" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Submission timestamp" },
        "status": { "type": "string", "enum": ["new", "reviewed", "resolved"], "description": "Current status of the feedback" }
      },
      "required": ["userId", "userName", "brainLabel", "content", "type", "createdAt", "status"]
    }
  },
  "firestore": {
    "feedback/{feedbackId}": {
      "schema": "Feedback",
      "description": "Collection of user feedback entries"
    }
  }
}
